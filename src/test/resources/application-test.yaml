spring:
#  config:
#    activate:
#      on-profile: jdbc
  datasource:
    url: jdbc:tc:postgresql://localhost:5432/postgres
    username: username
    password: password

#app:
#  database-access-type: jpa
